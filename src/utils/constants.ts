'use client'
export enum IndexedDBSettings {
    name = 'todos-db',
    tableTodos = 'todos',
    todosIndex = 'id',
    checkedAllIndex = 'checked',
    version = 1,
    tableCheckeAll = 'checkedAll'
}
